var commands = module.exports = {}
/**
 *
 * @param {Object} options
 */
commands.config = function(options) {

}
commands.config.description = 'Run the file server. Used for persistent storage'
commands.config.options = []